{{- if .Values.rollouts.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-security-mlops.fullname" . }}-canary
  labels:
    {{- include "network-security-mlops.labels" . | nindent 4 }}
    role: canary
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "network-security-mlops.selectorLabels" . | nindent 4 }}
{{- end }}
